# 🌐 Веб-интерфейс для Telegram Searcher

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка API (если еще не сделано)

Убедитесь, что в `config.py` указаны ваши API credentials:
```python
API_ID = ваш_api_id
API_HASH = 'ваш_api_hash'
```

### 3. Запуск веб-сервера

```bash
python app.py
```

### 4. Открыть в браузере

```
http://127.0.0.1:5000
```

## 📋 Функции веб-интерфейса

✅ **Добавление ключевых слов** - динамически через интерфейс  
✅ **Добавление городов** - динамически через интерфейс  
✅ **Настройка задержки** - в секундах между запросами  
✅ **Запуск поиска** - одной кнопкой  
✅ **Отслеживание статуса** - в реальном времени  
✅ **Скачивание результатов** - кнопки для скачивания Excel файлов  
✅ **Список всех файлов** - просмотр всех сохраненных результатов  

## 🎯 Использование

1. **Добавьте ключевые слова:**
   - Введите слово в поле "Ключевые слова"
   - Нажмите "Добавить" или Enter
   - Удалите слово, нажав × на теге

2. **Добавьте города:**
   - Введите название города
   - Нажмите "Добавить" или Enter
   - Удалите город, нажав × на теге

3. **Установите задержку:**
   - Введите значение в секундах (рекомендуется 5.0)
   - Нажмите "Установить"

4. **Запустите поиск:**
   - Нажмите "Начать поиск"
   - Статус будет обновляться автоматически

5. **Скачайте результаты:**
   - После завершения появятся кнопки "Скачать"
   - Или используйте раздел "Файлы результатов"

## ⚙️ Технические детали

- **Flask** - веб-фреймворк
- **Асинхронный поиск** - выполняется в отдельном потоке
- **Сессии** - каждый пользователь имеет свою конфигурацию
- **Автообновление статуса** - каждые 2 секунды

## 🔒 Безопасность

- API credentials читаются из `config.py`
- Сессии изолированы
- Файлы результатов хранятся в папке `results/`

## 📁 Структура

```
telegram_searcher/
├── app.py              # Flask приложение
├── telegram_searcher.py # Основной класс поиска
├── templates/
│   └── index.html      # Веб-интерфейс
├── results/            # Папка с результатами
└── config.py           # API credentials
```

## 🐛 Решение проблем

**Ошибка "API_ID и API_HASH не настроены":**
- Проверьте файл `config.py`
- Убедитесь, что указаны правильные значения

**Поиск не запускается:**
- Проверьте, что добавлены ключевые слова
- Убедитесь, что не запущен другой поиск

**Файлы не скачиваются:**
- Проверьте папку `results/`
- Убедитесь, что поиск завершился успешно





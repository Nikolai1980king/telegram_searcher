# 🔧 Техническое описание программы

## 📝 Название программы

**Рекомендуемые варианты:**

1. **Telegram Group Searcher** - простое и понятное
2. **TG Channel Finder** - короткое
3. **Telegram Community Searcher** - более общее
4. **TG Groups & Channels Finder** - описательное
5. **Telegram Search Bot** - простое

**Рекомендация:** `Telegram Group Searcher` или `TG Channel Finder`

---

## 🔍 На основе чего происходит поиск?

### Основная технология

**Telethon** - Python библиотека для работы с Telegram MTProto API

### API методы, которые используются:

#### 1. **SearchRequest** (основной метод)
```python
from telethon.tl.functions.contacts import SearchRequest

results = await client(SearchRequest(
    q=keyword,        # Поисковый запрос
    limit=50         # Максимальное количество результатов
))
```

**Что делает:**
- Ищет контакты, группы и каналы по ключевому слову
- Использует встроенный поиск Telegram
- Возвращает список найденных сущностей

**Ограничения:**
- Может вернуть flood wait при частых запросах
- Лимит результатов зависит от Telegram

#### 2. **GetFullChannelRequest** (для получения информации)
```python
from telethon.tl.functions.channels import GetFullChannelRequest

full_info = await client(GetFullChannelRequest(channel))
participants_count = full_info.full_chat.participants_count
```

**Что делает:**
- Получает полную информацию о канале/супергруппе
- Включает количество участников
- Требует дополнительный запрос для каждого канала

#### 3. **GetFullChatRequest** (для обычных групп)
```python
from telethon.tl.functions.messages import GetFullChatRequest

full_info = await client(GetFullChatRequest(chat_id))
participants_count = full_info.full_chat.participants_count
```

**Что делает:**
- Получает информацию о обычной группе
- Аналогично GetFullChannelRequest, но для групп

#### 4. **iter_dialogs** (альтернативный метод)
```python
async for dialog in client.iter_dialogs(limit=200):
    if keyword in dialog.entity.title:
        # Найдено
```

**Что делает:**
- Перебирает известные диалоги пользователя
- Ищет по названиям вручную
- Используется как fallback при flood wait

---

## 🏗️ Архитектура поиска

### Процесс поиска:

```
1. Генерация поисковых запросов
   ↓
   - Оригинальные ключевые слова
   - Транслитерированные (русские → английские)
   - Комбинации с городами
   - Транслит комбинаций

2. Для каждого запроса:
   ↓
   a) SearchRequest (основной метод)
      ├─ Успех → Обработка результатов
      └─ Flood Wait → Переход к (b)
   
   b) iter_dialogs (альтернативный метод)
      └─ Поиск по известным диалогам

3. Обработка результатов:
   ↓
   - Определение типа (группа/канал)
   - Получение количества участников
   - Удаление дубликатов
   - Сохранение в структуру данных

4. Сохранение в Excel:
   ↓
   - Отдельные файлы для групп и каналов
   - Форматирование с количеством участников
```

---

## 📊 Технические детали

### Используемые библиотеки:

1. **telethon** - работа с Telegram API
   - Версия: 1.39.0+
   - Протокол: MTProto
   - Тип: Асинхронная библиотека

2. **openpyxl** - работа с Excel
   - Версия: 3.1.2+
   - Формат: .xlsx
   - Функции: создание, форматирование, сохранение

### Типы данных Telegram:

- **Channel** - канал или супергруппа
  - `broadcast=True` → канал
  - `broadcast=False` → супергруппа
  
- **Chat** - обычная группа

### Обработка ошибок:

1. **Flood Wait** - временное ограничение
   - Обрабатывается автоматически
   - Пропускается запрос с большим ожиданием (>1 часа)
   - Переход на альтернативный метод

2. **Network Errors** - сетевые ошибки
   - Повторная попытка
   - Логирование ошибки

3. **API Errors** - ошибки API
   - Обработка исключений
   - Продолжение работы

---

## 🔐 Авторизация

### Процесс:

1. **API Credentials** (из my.telegram.org):
   - `api_id` - идентификатор приложения
   - `api_hash` - хеш приложения

2. **Сессия** (при первом запуске):
   - Номер телефона
   - Код подтверждения
   - Пароль 2FA (если включен)

3. **Сохранение сессии**:
   - Файл `telegram_session.session`
   - Повторная авторизация не требуется

---

## ⚙️ Настройки и оптимизация

### Параметры производительности:

1. **SEARCH_DELAY** - задержка между запросами
   - Влияет на скорость и риск flood wait
   - Рекомендуется: 2.0-3.0 секунды

2. **LIMIT_PER_KEYWORD** - лимит результатов
   - Максимальное количество на запрос
   - По умолчанию: 50

3. **USE_TRANSLITERATION** - транслитерация
   - Увеличивает количество запросов
   - Помогает найти группы с транслитом

4. **USE_CITY_COMBINATIONS** - комбинации с городами
   - Экспоненциально увеличивает запросы
   - Максимальный охват поиска

---

## 📈 Производительность

### Оценка времени:

- **1 запрос** = ~0.5-3.0 секунд (в зависимости от задержки)
- **1000 запросов** = ~8-50 минут
- **10,000 запросов** = ~1.5-8 часов
- **40,000 запросов** = ~5-22 часа

### Ограничения:

- **Telegram API лимиты**: ~20-30 запросов/минуту
- **Flood wait**: при превышении лимитов
- **Сетевые задержки**: зависят от соединения

---

## 🎯 Для программиста - краткое описание

### Что делает программа:

**"Программа использует Telethon (Python библиотека для Telegram MTProto API) для автоматического поиска групп и каналов по ключевым словам. Основной метод поиска - SearchRequest из Telegram API, который позволяет искать контакты, группы и каналы по текстовому запросу. Для получения дополнительной информации (количество участников) используется GetFullChannelRequest/GetFullChatRequest. Результаты сохраняются в Excel файлы с помощью openpyxl."**

### Ключевые технологии:

- **Telethon** - Telegram API клиент
- **MTProto** - протокол Telegram
- **SearchRequest** - метод поиска
- **openpyxl** - работа с Excel

### Основной алгоритм:

1. Генерация поисковых запросов (ключевые слова + транслитерация + города)
2. Для каждого запроса: вызов SearchRequest
3. Обработка результатов: определение типа, получение метаданных
4. Сохранение в Excel: отдельные файлы для групп и каналов

---

## 📝 Краткое описание для README

```markdown
## Технические детали

Программа использует:
- **Telethon** (v1.39.0+) - Python библиотека для Telegram MTProto API
- **SearchRequest** - основной метод поиска групп и каналов
- **GetFullChannelRequest/GetFullChatRequest** - получение метаданных
- **openpyxl** (v3.1.2+) - сохранение результатов в Excel

Поиск происходит через официальный Telegram API, используя встроенный 
поиск Telegram по ключевым словам. Программа автоматически обрабатывает 
транслитерацию, создает комбинации с городами и получает количество 
участников для каждой найденной группы/канала.
```

---

**Для программиста:** Программа - это автоматизированный клиент Telegram API, который использует SearchRequest для поиска и GetFullChannelRequest для получения метаданных, с сохранением результатов в Excel.







# ⚠️ Flood Wait - Что это и как с этим бороться

## Что такое Flood Wait?

**Flood Wait** - это временное ограничение от Telegram API, которое возникает когда:
- Вы делаете слишком много запросов за короткое время
- Telegram считает, что вы злоупотребляете API
- Система защищает серверы от перегрузки

## Ваша ошибка

```
A wait of 19004 seconds is required
```

Это означает:
- ⏱️ Нужно подождать **19004 секунд** = **~5.3 часа**
- 🔒 Telegram временно заблокировал ваш аккаунт для этого типа запросов
- ⚠️ Слишком много запросов было сделано слишком быстро

## Почему это произошло?

1. **Слишком много поисковых запросов** - у вас 47 ключевых слов × 108 городов = ~40,000+ запросов
2. **Маленькая задержка** - `SEARCH_DELAY = 0.5` секунды может быть недостаточно
3. **Telegram ограничил** - система защиты сработала

## Что делать?

### Вариант 1: Подождать (рекомендуется)

Просто подождите указанное время (в вашем случае ~5.3 часа), затем:
- Запустите программу снова
- Увеличьте задержку между запросами

### Вариант 2: Увеличить задержку

Откройте `config.py` и увеличьте задержку:

```python
# Было:
SEARCH_DELAY = 0.5

# Стало:
SEARCH_DELAY = 2.0  # или даже 3.0
```

### Вариант 3: Уменьшить количество запросов

1. **Уменьшите количество городов:**
```python
CITIES = [
    "Москва",
    "Санкт-Петербург",
    "Новосибирск",
    # Оставьте только 10-20 крупных городов
]
```

2. **Уменьшите LIMIT_PER_KEYWORD:**
```python
LIMIT_PER_KEYWORD = 20  # Вместо 50
```

3. **Отключите комбинации с городами временно:**
```python
USE_CITY_COMBINATIONS = False  # Только ключевые слова
```

### Вариант 4: Обработка flood wait в коде

Программа автоматически переключается на альтернативный метод поиска (по известным диалогам), но это менее эффективно.

## Как избежать flood wait в будущем?

### Рекомендуемые настройки:

```python
# Для большого количества запросов (40,000+)
SEARCH_DELAY = 2.0  # или 3.0 секунды

# Для среднего количества (5,000-10,000)
SEARCH_DELAY = 1.0  # секунда

# Для малого количества (<1,000)
SEARCH_DELAY = 0.5  # полсекунды
```

### Оптимальная стратегия:

1. **Начните с малого:**
   - 5-10 ключевых слов
   - 5-10 городов
   - `SEARCH_DELAY = 1.0`

2. **Постепенно увеличивайте:**
   - Если все работает, добавьте больше городов
   - Если получаете flood wait, увеличьте задержку

3. **Разбейте на части:**
   - Запускайте поиск по частям (например, по 10 городов за раз)
   - Делайте перерывы между запусками

## Текущая ситуация

С вашими настройками:
- 47 ключевых слов
- 108 городов
- ~40,000+ запросов
- Задержка 0.5 секунды

**Рекомендация:** Увеличьте задержку до 2.0-3.0 секунд или уменьшите количество городов.

## Что происходит сейчас?

Программа автоматически переключилась на альтернативный метод поиска (по известным диалогам), но:
- ⚠️ Это менее эффективно
- ⚠️ Найдет только те группы/каналы, которые уже есть в ваших диалогах
- ⚠️ Не найдет новые группы/каналы

## План действий

1. **Сейчас:** Подождите ~5 часов или используйте альтернативный поиск
2. **После:** Измените настройки в `config.py`:
   ```python
   SEARCH_DELAY = 2.0  # Увеличьте задержку
   CITIES = CITIES[:20]  # Оставьте только первые 20 городов
   ```
3. **Запустите снова** с новыми настройками

## Важно

- ✅ Flood wait **временный** - через указанное время все вернется в норму
- ✅ Это **не блокировка аккаунта** - просто ограничение на запросы
- ✅ Можно **продолжить поиск** после ожидания
- ✅ **Увеличьте задержку** для предотвращения в будущем

---

**Совет:** Для такого большого количества запросов лучше разбить поиск на несколько запусков с перерывами между ними.







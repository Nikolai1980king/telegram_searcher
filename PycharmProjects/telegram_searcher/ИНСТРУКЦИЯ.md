# 📖 Инструкция по использованию

## Быстрый старт

### Шаг 1: Установка библиотек

```bash
pip install -r requirements.txt
```

### Шаг 2: Получение API ключей

1. Откройте https://my.telegram.org в браузере
2. Войдите в свой аккаунт Telegram (введите номер телефона)
3. Введите код подтверждения из Telegram
4. Перейдите в раздел **"API development tools"**
5. Заполните форму:
   - App title: любое название (например, "Telegram Searcher")
   - Short name: короткое название (например, "tsearcher")
   - Platform: выберите "Desktop"
   - Description: можно оставить пустым
6. Нажмите "Create application"
7. Скопируйте:
   - **api_id** (число, например: 12345678)
   - **api_hash** (строка, например: "abcdef1234567890abcdef1234567890")

### Шаг 3: Настройка конфигурации

1. Скопируйте файл `config_example.py` в `config.py`:
   ```bash
   cp config_example.py config.py
   ```

2. Откройте `config.py` и заполните:
   ```python
   API_ID = 12345678  # Ваш api_id
   API_HASH = 'ваш_api_hash'  # Ваш api_hash
   
   KEYWORDS = [
       'python',
       'программирование',
       'технологии',
       # Добавьте свои ключевые слова
   ]
   ```

### Шаг 4: Запуск

```bash
python telegram_searcher.py
```

При первом запуске:
1. Введите номер телефона (с кодом страны, например: +79991234567)
2. Введите код подтверждения из Telegram
3. Если включена двухфакторная аутентификация, введите пароль

**Важно:** Сессия сохранится в файле `telegram_session.session`. При следующих запусках авторизация не потребуется.

### Шаг 5: Результаты

После завершения поиска будут созданы два Excel файла:
- `telegram_groups_YYYYMMDD_HHMMSS.xlsx` - найденные группы
- `telegram_channels_YYYYMMDD_HHMMSS.xlsx` - найденные каналы

## Настройка поиска

В файле `config.py` можно изменить:

- **KEYWORDS** - список ключевых слов для поиска
- **LIMIT_PER_KEYWORD** - максимальное количество результатов на одно ключевое слово (по умолчанию 50)

## Примеры использования

### Поиск по техническим темам:
```python
KEYWORDS = ['python', 'javascript', 'ai', 'machine learning', 'blockchain']
```

### Поиск по бизнес-темам:
```python
KEYWORDS = ['стартап', 'бизнес', 'инвестиции', 'предпринимательство']
```

### Поиск по хобби:
```python
KEYWORDS = ['фотография', 'путешествия', 'кулинария', 'спорт']
```

## Решение проблем

### Ошибка: "Необходимо настроить API_ID и API_HASH"
- Убедитесь, что создали файл `config.py`
- Проверьте, что правильно скопировали api_id и api_hash

### Ошибка авторизации
- Проверьте правильность номера телефона (должен быть с кодом страны)
- Убедитесь, что код подтверждения введен правильно
- Если включена 2FA, введите пароль

### Мало результатов
- Попробуйте более общие ключевые слова
- Увеличьте `LIMIT_PER_KEYWORD` в config.py
- Используйте ключевые слова на разных языках

### Ошибка при сохранении Excel
- Закройте открытые Excel файлы
- Проверьте права на запись в директорию

## Безопасность

⚠️ **Важно:**
- НЕ делитесь файлом `config.py` - он содержит ваши API ключи
- НЕ делитесь файлом `telegram_session.session` - он содержит вашу авторизацию
- НЕ коммитьте эти файлы в Git (они уже в .gitignore)

## Дополнительная информация

- Документация Telethon: https://docs.telethon.dev/
- Получение API ключей: https://my.telegram.org
- Поддержка: создайте issue в репозитории проекта







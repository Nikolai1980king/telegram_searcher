# Telegram Searcher - Автоматический поиск групп и каналов

Автоматизированный поиск групп и каналов в Telegram с сохранением результатов в отдельные Excel файлы.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Получение API credentials

1. Перейдите на https://my.telegram.org
2. Войдите в свой аккаунт Telegram
3. Перейдите в раздел "API development tools"
4. Создайте приложение и получите:
   - `api_id` (число)
   - `api_hash` (строка)

### 3. Настройка конфигурации

Создайте файл `config.py` на основе `config_example.py`:

```python
API_ID = 12345678  # Ваш API ID
API_HASH = 'your_api_hash_here'  # Ваш API Hash
KEYWORDS = ['python', 'programming', 'tech']  # Ваши ключевые слова
```

### 4. Запуск

```bash
python telegram_searcher.py
```

При первом запуске потребуется:
- Ввести номер телефона
- Ввести код подтверждения из Telegram
- При необходимости ввести пароль двухфакторной аутентификации

Сессия сохранится в файле `telegram_session.session`, повторная авторизация не потребуется.

## 📋 Функции

- ✅ Автоматический поиск по ключевым словам
- ✅ Разделение групп и каналов
- ✅ Сохранение в отдельные Excel файлы
- ✅ Удаление дубликатов
- ✅ Красивое форматирование Excel файлов
- ✅ Информация о количестве участников/подписчиков

## 📁 Структура проекта

```
telegram_searcher/
├── telegram_searcher.py  # Основной скрипт
├── config_example.py      # Пример конфигурации
├── requirements.txt       # Зависимости
├── README.md             # Документация
└── VARIANTS.md           # Описание вариантов решения
```

## ⚙️ Настройки

В файле `telegram_searcher.py` можно изменить:

- `limit_per_keyword` - максимальное количество результатов на ключевое слово
- Имена выходных файлов
- Формат сохранения данных

## 🔒 Безопасность

- **НЕ коммитьте** файл `config.py` в Git
- **НЕ делитесь** файлом `telegram_session.session` - он содержит вашу авторизацию
- Добавьте в `.gitignore`:
  ```
  config.py
  *.session
  *.xlsx
  ```

## 📝 Пример использования

```python
from telegram_searcher import TelegramSearcher
import asyncio

async def search():
    searcher = TelegramSearcher(API_ID, API_HASH)
    await searcher.connect()
    
    results = await searcher.search_channels_and_groups(
        ['python', 'programming'],
        limit_per_keyword=50
    )
    
    searcher.save_to_excel(
        results['groups'],
        results['channels']
    )
    
    await searcher.disconnect()

asyncio.run(search())
```

## ❓ Часто задаваемые вопросы

**Q: Нужен ли мне номер телефона?**  
A: Да, для авторизации в Telegram API требуется номер телефона.

**Q: Безопасно ли это?**  
A: Да, используется официальный Telegram API. Ваши данные не передаются третьим лицам.

**Q: Можно ли искать на других языках?**  
A: Да, просто используйте ключевые слова на нужном языке.

**Q: Что делать, если поиск не находит результаты?**  
A: Попробуйте другие ключевые слова или увеличьте `limit_per_keyword`.

## 🐛 Решение проблем

**Ошибка авторизации:**
- Проверьте правильность API_ID и API_HASH
- Убедитесь, что номер телефона введен с кодом страны

**Мало результатов:**
- Попробуйте более общие ключевые слова
- Увеличьте `limit_per_keyword`

**Ошибка при сохранении Excel:**
- Убедитесь, что файлы не открыты в другой программе
- Проверьте права на запись в директорию







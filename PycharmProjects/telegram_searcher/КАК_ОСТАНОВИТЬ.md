# 🛑 Как остановить поиск

## Вариант 1: Консольная версия (telegram_searcher.py)

### Быстрый способ

**Нажмите в терминале:**
```
Ctrl + C
```

Или дважды (если не сработало с первого раза):
```
Ctrl + C
Ctrl + C
```

### Что происходит при остановке?

✅ **Программа корректно завершится:**
- Сохранит все уже найденные группы и каналы
- Создаст Excel файлы с префиксом `interrupted_`
- Корректно отключится от Telegram

### Пример

```
📝 Ищу: 'Знакомства Москва'...
  👥 Группа: Знакомства Москва [12 345 участников]
  📢 Канал: Знакомства в Москве [45 678 подписчиков]
^C

⚠️ Поиск прерван пользователем (Ctrl+C)
💾 Сохраняю уже найденные результаты...

✅ Найденные результаты сохранены:
   📁 telegram_groups_interrupted_20241223_143022.xlsx (15 групп)
   📁 telegram_channels_interrupted_20241223_143022.xlsx (23 канала)

👋 Отключено от Telegram
```

---

## Вариант 2: Веб-версия (app.py)

### Способ 1: Через веб-интерфейс (рекомендуется)

1. Откройте браузер: `http://127.0.0.1:5000`
2. Нажмите кнопку **"Остановить поиск"**
3. Результаты автоматически сохранятся в папку `results/`

### Способ 2: Остановить Flask сервер

**Нажмите в терминале, где запущен Flask:**
```
Ctrl + C
```

⚠️ **Внимание:** Это остановит весь веб-сервер, а не только поиск!

### Способ 3: Найти и остановить процесс поиска

Если нужно остановить только поиск, но оставить сервер работающим:

```bash
# Найти процесс Flask
ps aux | grep "python.*app.py"

# Остановить процесс (замените PID на реальный номер процесса)
kill -SIGINT <PID>
```

Или найти и остановить все процессы Python:
```bash
# Найти все процессы Python, связанные с app.py
pkill -f "python.*app.py"
```

---

## Важно

### Для консольной версии:
- ✅ **Результаты сохранятся** - все найденные группы/каналы будут в Excel
- ✅ **Можно продолжить позже** - запустите программу снова
- ✅ **Безопасно** - программа корректно завершит работу

### Для веб-версии:
- ✅ **Результаты сохранятся** - при остановке через интерфейс результаты сохраняются автоматически
- ⚠️ **При остановке сервера (Ctrl+C)** - результаты тоже сохранятся, если поиск был запущен
- ✅ **Можно перезапустить** - просто запустите `python3 app.py` снова

---

## Альтернативные способы остановки

### В Linux/Mac:
- `Ctrl + C` - прервать программу
- `Ctrl + Z` - приостановить (программа останется в фоне)
  - Чтобы вернуть: `fg`
  - Чтобы убить: `kill %1`

### В Windows:
- `Ctrl + C` - прервать программу
- Закрыть окно терминала

---

## Если программа не останавливается

1. Попробуйте нажать `Ctrl + C` дважды
2. Если не помогает, закройте терминал
3. Результаты все равно сохранятся (если программа успела что-то найти)

---

## Продолжение поиска

После остановки можно:
1. Запустить программу снова - она продолжит поиск
2. Или изменить ключевые слова в `config.py` и запустить заново

---

**Совет:** Если поиск занимает слишком много времени, можно:
- Уменьшить количество городов в `config.py`
- Уменьшить `LIMIT_PER_KEYWORD`
- Увеличить `SEARCH_DELAY` (чтобы избежать flood wait)
- Остановить и запустить с другими настройками
